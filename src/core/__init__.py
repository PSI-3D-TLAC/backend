"""Core domain package for the 3D PrintHub backend.

Each subpackage represents a bounded context with entities, services, and an
optional repository. Routes (in src/server) stay thin and delegate here.
"""
